# Core
python-dotenv>=1.0.0
pyyaml>=6.0

# Spotify API
spotipy>=2.23.0

# Data Processing
pandas>=2.1.4
numpy>=1.26.2
pyarrow>=22.0.0

# Orchestration
apache-airflow>=3.0.0

# Azure SDK
azure-storage-blob>=12.19.0
azure-functions>=1.18.0

# API
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3

# ML
scikit-learn>=1.4.0
scipy>=1.11.4

# Dashboard
streamlit>=1.30.0
plotly>=5.0.0

# Testing
pytest>=7.4.3
pytest-cov>=4.1.0
httpx>=0.27.0

# Observability
structlog>=24.0.0
apache-airflow-providers-openlineage>=2.0.0
azure-monitor-opentelemetry>=1.6.0

# Transformation (gold layer)
dbt-core>=1.7.0
dbt-duckdb>=1.7.0
# dbt-postgres is the future target (see dbt/profiles.yml `pg` output).
# Kept installed so users can flip DBT_TARGET=pg without rebuilding the image.
dbt-postgres>=1.7.0

# Utils
requests>=2.31.0
